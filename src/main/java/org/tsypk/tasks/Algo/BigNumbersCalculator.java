package org.tsypk.tasks.Algo;

import java.util.Collections;
import java.util.List;

/**
 * Целые числа задаются как список цифр без лидирующих нулей
 * a = 5489 -> [5, 4, 8, 9];
 * b = 123 -> [1, 2, 3];
 * результат [5, 6, 1, 2]
 * Нужно реализовать сложение чисел, которое возвращает ответ в том же формате
 */
public class BigNumbersCalculator {
    /**
     * @param a - первое число
     * @param b - второе число
     * @return сумма [a + b]
     */
    public List<Integer> plus(List<Integer> a, List<Integer> b) {
        return Collections.emptyList();
    }
}
